// @ts-check
import { defineConfig } from 'astro/config';

// Sitio de usuario de GitHub Pages -> se sirve en la raíz del dominio.
export default defineConfig({
  site: 'https://trickmiin.github.io',
});
